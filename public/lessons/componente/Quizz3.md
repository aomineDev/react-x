# QUIZZ: Expresiones en JSX

Completa los espacios marcados con `/* ??? */` para que el código funcione correctamente.

---

1. Muesta el nombre del empleado
2. Muestra la edad
3. Muestra su salario actual, mas un 20% por bono navideño

```jsx
function Perfil() {
  const nombre = "carlos";
  const edad = 30;
  const salario = 2500;

  return (
    <div>
      <h1>Empleado {/* ??? */}</h1>
      <p>Edad: {/* ??? */}</p>
      <p>Salario: {/* ??? */}</p>
    </div>
  );
}
```
