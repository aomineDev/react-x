# QUIZZ 5 – Emparejamiento: Objetos y Arrays en useState

Columna A – Conceptos

1. setState({...state, nuevoValor })

2. setState(prev => prev.map(...))

3. setState(prev => [...prev, nuevoItem])

4. setState(prev => prev.filter(...))

5. Inmutabilidad

Columna B – Descripciones

A. Agregar un nuevo elemento a un array sin mutarlo.
B. Actualizar un objeto creando una copia superficial del anterior.
C. El principio clave que evita modificar directamente el estado.
D. El patrón correcto para editar un elemento dentro de un array.
E. El patrón correcto para eliminar elementos de un array.

(4 - E), (1 - D), (3-A), (2 - C) ( 5 - B)
