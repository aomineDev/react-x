# QUIZZ 3 — Selección de múltiples respuestas

Selecciona la opción corrrecta.

**Pregunta**

¿Cuál es la razón más común para usar `e.preventDefault()` en un formulario con React?

**Opciones**

- A) Para evitar que React borre el estado del componente.
- B) Para evitar que el navegador recargue la página al enviar el formulario. \*
- C) Para evitar que el usuario escriba números en un input.
- D) Para impedir que se renderice el componente nuevamente.
