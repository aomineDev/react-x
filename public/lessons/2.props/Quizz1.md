# QUIZZ: Completar codigo

Completa los espacios marcados con `/* ??? */` para que el código funcione correctamente.

```jsx
function Persona(/* ??? */) {
  return (
    <div>
      <h2>Nombre: {props./* ??? */}</h2>
      <p>Edad: {props./* ??? */} años</p>
    </div>
  );
}

export default function App() {
  return <Persona nombre="Luis" edad={22} />;
}
```
